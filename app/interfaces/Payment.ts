export enum PaymentProvider {
  stripe = "stripe",
  vipps = "vipps",
}

export enum Plan {
  starter = "starter",
  basic = "basic",
  plus = "plus",
}

export enum PlanPrice {
  starter = 29,
  basic = 59,
  plus = 99,
}
